// ─── App Configuration Constants ─────────────────────────────────────────────
// Adjust these values to match your contest setup.

/** Emails that have admin access to manage fixtures and scores */
export const ADMIN_EMAILS = ['safwan4624@gmail.com']; // Replace with real admin emails

/** Hours before kickoff when predictions lock */
export const PREDICTION_LOCK_HOURS = 1;

/** Hours before kickoff when predictions open */
export const PREDICTION_OPEN_HOURS = 72;

/** Points awarded for predicting the correct outcome (Win/Draw/Loss) */
export const POINTS_CORRECT_OUTCOME = 1;

/** Bonus points awarded for predicting the exact score (Total = 3) */
export const POINTS_EXACT_SCORE = 2;

/** App branding */
export const APP_NAME = 'GRAPHWHITE FIFA WORLD CUP 2026 ';
export const APP_DESCRIPTION = 'FIFA World Cup 2026 Graphwhite Prediction Contest';
